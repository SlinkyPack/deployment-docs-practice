# משימות תרגול

שמונה משימות, מהקלה לקשה. כל משימה עוברת את חמשת השלבים המלאים: ענף חדש, עריכה, commit, push, ופתיחת Pull Request.

אפשר לעשות אחת ביום, ואפשר את כולן ברצף. אין לחץ, ואי אפשר לשבור כלום.
שדגכגשדגכשדגכגשדכגשד
---

## 1. חימום — לשנות טקסט

**הקובץ:** `docs/power-specs.md`
**המשימה:** בשורת ההערות בסוף, לשנות את המילה `minimums` ל-`minimum values`.
**שם הענף:** `fix-fuse-wording`
**סימן הצלחה:** התצוגה המקדימה מראה את הנוסח החדש, וב-GitHub Desktop רואים שורה אחת אדומה ושורה אחת ירוקה.

---

## 2. להוסיף שורה לטבלה

**הקובץ:** `docs/power-specs.md`
**המשימה:** להוסיף לטבלת ה-Standard units שורה חדשה: `FU-300`, מתח `24 V DC`, זרם נומינלי `1.9 A`, זרם שיא `3.8 A`, נתיך `8 A`.
**שם הענף:** `add-fu300-specs`
**סימן הצלחה:** הטבלה בתצוגה המקדימה מציגה ארבע שורות, והעמודות מיושרות.

> אם הטבלה נשברה — כנראה חסר קו אנכי `|` בהתחלה או בסוף השורה. ספרי את הקווים: צריך שיהיה אותו מספר בכל השורות.

---

## 3. להוסיף שלב לדיאגרמה

**הקובץ:** `docs/deployment-guide.md`
**המשימה:** בדיאגרמה הראשונה, להוסיף שלב בין `Run wiring` לבין `Connect network` שנקרא `Insulation test`.
**שם הענף:** `add-insulation-test-step`

איך עושים את זה: כרגע יש בדיאגרמה שורה `F --> G`. צריך להחליף אותה בשתי שורות:

```
    F --> W[Insulation test]
    W --> G
```

**סימן הצלחה:** בתצוגה המקדימה יש עכשיו תיבה נוספת בין השתיים, והחצים עוברים דרכה.

---

## 4. אבחון — למה זה לא נראה נכון

**הקובץ:** `docs/site-checklist.md`
**המשימה:** פתחי את הקובץ בתצוגה מקדימה והסתכלי על הסעיף `Pre-power checks`. הוא נראה שונה מכל שאר הסעיפים. מצאי למה ותקני.
**שם הענף:** `fix-checklist-formatting`

> רמז: השווי את הסעיף הזה לסעיף שמעליו, שורה מול שורה. ההבדל הוא בדבר אחד שאי אפשר לראות בטקסט עצמו.

**סימן הצלחה:** הרשימה מופיעה עם נקודות, בדיוק כמו בשאר הסעיפים.

---

## 5. להוסיף סעיף שלם

**הקובץ:** `docs/deployment-guide.md`
**המשימה:** להוסיף סעיף חדש בשם `8. Common faults` בסוף המסמך, עם רשימה של שלוש תקלות נפוצות ומה עושים בכל אחת. את התוכן אפשר להמציא — זה תרגיל בפורמט, לא בהנדסה.
**שם הענף:** `add-common-faults`
**סימן הצלחה:** הכותרת מופיעה בגודל הנכון, הרשימה מופיעה עם נקודות, ויש שורה ריקה לפני ואחרי כל אחת מהן.

---

## 6. לעבוד עם Claude

**הקובץ:** `docs/site-checklist.md`
**המשימה:** לבקש מ-Claude בפאנל: ״הוסף לקובץ הזה סעיף חדש בשם Winter conditions עם ארבעה פריטי בדיקה שרלוונטיים למזג אוויר קר״.
**שם הענף:** `add-winter-checklist`

**מה חשוב כאן:** לקרוא את מה שהוא הציע **לפני** שלוחצים Accept. אם משהו לא מתאים — לבקש ממנו לתקן, או לתקן בעצמך. את חותמת על השינוי.

**סימן הצלחה:** הסעיף נוסף, הוא נראה כמו שאר הסעיפים, ואת יכולה להסביר מה כל שורה בו אומרת.

---

## 7. שתי דיאגרמות בבת אחת

**הקובץ:** `docs/deployment-guide.md`
**המשימה:** בדיאגרמת החיווט (השנייה במסמך), להוסיף רכיב `LED[Status LED]` שמחובר מ-`CTRL`. בנוסף, בדיאגרמת הרצף (השלישית), להוסיף שורה אחרונה שבה הטכנאי מוסר דוח ללקוח.
**שם הענף:** `expand-wiring-and-sequence`
**סימן הצלחה:** שתי הדיאגרמות עדיין נבנות כציור. אם אחת מהן הפכה לטקסט — Ctrl+Z וננסה שוב לאט.

---

## 8. הלולאה המלאה, בלי להסתכל במדריך

**המשימה:** בחרי בעצמך שיפור קטן כלשהו באחד המסמכים — ניסוח, סעיף חסר, טבלה שאפשר להבהיר — ובצעי אותו מקצה לקצה בלי להציץ במדריך.
**שם הענף:** את בוחרת. תני שם שמתאר את השינוי.

**סימן הצלחה:** ה-PR פתוח, התיאור בו מסביר מה שינית ולמה, ולא הצצת באף שלב.
---
## 9 תרגיל
עדכון בענף שלנו, (עדכון של מנהל ריפו), FETCH 
לראות תוצאה ממחברת למחבר למחברות
לפני הרצה ניחוש התוצאה.
## א2
asdasd
asdasdasdas
מחברותתתתתתתת
זכרי את הצעד שקל לשכוח: אחרי שה-PR אושר ומוזג, חוזרים ל-`main`, עושים `Pull`, ורק אז פותחים ענף חדש למשימה הבאה.
